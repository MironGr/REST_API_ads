Run через PyCharm src/app.py

___________


Для '/ads'
# Тело запроса (Body - row - TEXT)
     Query string:
     seller_id: int?
     tags: str?
     make: str?
     model: str?

Для '/users/<int:id>/ads'
# Тело запроса (Body - row - TEXT)
     Query string:
     tags: str?
     make: str?
     model: str?